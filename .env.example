PORT=3100
HOST=0.0.0.0

# Comma-separated list of origins allowed to embed this service as an iframe.
# Example: ALLOWED_ORIGINS=https://yoursite.com,https://www.yoursite.com
# Leave empty to allow all origins (suitable for local dev / fully public deployment).
ALLOWED_ORIGINS=
